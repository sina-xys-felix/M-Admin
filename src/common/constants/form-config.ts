/**
 * 表单(项)常用配置
 * */
const FORM_CONFIG = {
  /**
   *  是否显示冒号
   * */
  showColon: true,
  /**
   * 标签的对齐方向
   * */
  labelAlign: 'left',
  /**
   * 表单的布局方式，包括水平、垂直、多列
   * */
  layout: 'vertical',
  /**
   * 内容层是否开启 flex 布局
   * */
  contentFlex: true,
  /**
   * 是否隐藏星号
   * */
  hideAsterisk: false,
  /**
   * 可选择将星号置于 label 前/后
   * */
  asteriskPosition: 'end',
}

export { FORM_CONFIG }
